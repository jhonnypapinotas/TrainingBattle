#
# Build configuration for Circle CI
#

general:
    artifacts:
        - /home/ubuntu/TrainingBattle/app/build/outputs/apk/

machine:
    java:
        version: oraclejdk8
test:
    override:
        - (./gradlew assemble):
            timeout: 360